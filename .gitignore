# Cache files
f1_cache.json
upcoming_launches_cache.json
# previous_launches_cache.json  # Now tracked in git as source of truth (only additions, with backup fallback)
launch_cache.json
cache/f1_cache.json
cache/f1_*.json  # Old F1 cache files

# Keep track images and other assets in cache/
!cache/tracks/
!cache/README_previous_launches_cache.md

# Log files
app.log
app_launch.log

# Python cache
__pycache__/
*.pyc
*.pyo

# Virtual environments
.venv/
venv/
env/

# IDE files
.idea/
.vscode/

# OS files
.DS_Store
Thumbs.db

# Temporary files
*.tmp
*.temp

# Persistent cache (outside git repo)
# F1 data is now cached in ~/.cache/spacex-dashboard/ for better performance
.idea/dashboard.iml
.idea/misc.xml
